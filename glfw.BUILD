cc_library(
    name = "glfw",
    #srcs = glob(["**"]),
    hdrs = glob(["include/**/*.h"]),
    #includes = ["include"],
    deps = ["@vulkan//:vulkan"],
    visibility = ["//visibility:public"],
    strip_include_prefix = "include",
)
